# SQL Docs

SQL files are organized by purpose.

## Structure

- `migrations/`: Ordered migration SQL files
- `scripts/`: Manual or one-off SQL scripts
- `supabase_schema.sql`: Current schema snapshot/export
